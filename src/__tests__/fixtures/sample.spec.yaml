feature: sample module
code: sample
description: Тестовый модуль

specs-unit:
  Регистрация:
    - assert: Создаёт инстанс через конструктор
    - assert: Выбрасывает ошибку при невалидных параметрах

  Кэширование:
    - assert: Кэширует результат при повторном вызове
    - assert: Инвалидирует кэш по TTL

  Хранилище:
    Redis:
      - assert: Подключается к Redis
    Memory:
      - assert: Хранит данные в памяти

specs-e2e:
  Полный сценарий:
    - assert: Пользователь создаёт объект и видит его в списке
